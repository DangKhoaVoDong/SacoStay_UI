/** Dùng khi `ng build --configuration production` (fileReplacements). */
export const environment = {
  production: true,
  apiUrl: 'https://api.sacostay.id.vn/api',
  appUrl: 'https://sacostay.id.vn',
  chatHubUrl: 'https://api.sacostay.id.vn/chatHub'
};
